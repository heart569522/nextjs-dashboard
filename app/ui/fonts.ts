import { Noto_Sans_Thai, Lusitana } from 'next/font/google';

export const noto_sans_thai = Noto_Sans_Thai({ subsets: ['thai'] });

export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'],
  });